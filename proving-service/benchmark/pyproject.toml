[project]
name = "proving-benchmark"
version = "0.1.0"
description = "Benchmark proving service"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "matplotlib>=3.7.0",
    "numpy>=1.24.0",
]
